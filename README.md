
  # Dynamic Gradient Background

  This is a code bundle for Dynamic Gradient Background. The original project is available at https://www.figma.com/design/cbsovW5a4uEdenmPh5zhxO/Dynamic-Gradient-Background.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  